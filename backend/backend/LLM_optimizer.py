import time
def shiftOptimizer(data):
    # time.sleep(5)  # simulate some processing time
    response = [{"employee": d["employee"], "shift": "optimized shifts json is here"} for d in data]
    return response